# Daily SEO Report — 2026-09-21

## Shipped today

**Task:** `[AUTO]` Add JSON-LD `Review`/testimonial markup for the homepage testimonials section. (Phase 1)

- Added a `Review` JSON-LD block (as a `@graph` array) to the `<head>` of `index.html`, one `Review` object per testimonial in the "What Clients Say" (`#proof`) section — 4 total: Pankaj Bhatt (MPL), Vinod Guneja (Angara E-commerce), Mukul Bhaisora (QualityLabs), Nishank Agarwal (Creatiosoft).
- Each `Review.reviewBody` is copied verbatim from the visible quote text (HTML entities converted to plain text, no rewording), `author` is a `Person` with `jobTitle` and `worksFor` sourced from the visible name/role line, and `itemReviewed` points at the QAInfinity `Organization` (same name/url as the existing `Organization` block).
- Deliberately **omitted** `reviewRating` — the page displays no star rating or numeric score anywhere in the testimonials section, so adding one would be fabricating data not present on the page (against Google's structured-data matching guidance and this project's no-fabrication guardrail).

**Commit:** [`8b5ffb2`](https://github.com/nitesh-qain/qain-website/commit/8b5ffb2) — pushed directly to `main`.

**Verification:** Ran a real headless-Chromium pass (Playwright, via the pre-installed sandbox browser) against all 5 pages (`index.html` + 3 case studies + case-studies hub) served from a local static server, at both desktop (1280×800) and mobile (375×812) viewports:
- All 5 pages returned HTTP 200.
- Zero broken internal links found on any page.
- Key interactive elements on the homepage (nav links, FAQ toggle) present and clickable without throwing.
- All 3 JSON-LD `<script>` blocks in `index.html` parsed as valid JSON with the expected types (`Organization`, `FAQPage`, and the new `Review` ×4 inside a `@graph`).
- Diffed the change to confirm it's purely additive (51 insertions, 0 deletions) — only a new `<script>` block was added to `<head>`; the visible testimonials markup itself was untouched.
- Two pre-existing issues, unrelated to this change, were checked and confirmed **not** regressions: (a) console errors from Google Tag Manager are caused by the sandbox's network egress policy blocking that third-party domain — present on every page before this change too; (b) the mobile-viewport overflow flag on `index.html` and `case-studies/automation.html` was reproduced identically against the pre-edit version of `index.html` (via `git stash`), confirming it's the same pre-existing issue noted in yesterday's report and still tracked under the upcoming Core Web Vitals task (Phase 1), not something introduced or fixed today.

## Opened for review

None today — no `[PR]` blog task was reached (the next unchecked task in PLAN.md was the `[AUTO]` Review-markup item above).

## Blocked / not shipped

None — the task completed and verified successfully on the first attempt.

## Research findings

Not a Phase 3 research day.

## LinkedIn draft

Not applicable — no blog post or notable milestone shipped today (this was a technical SEO / structured-data task, not customer-facing content).

## Reminders — `[NEEDS-YOU]` items skipped

Passed over while scanning PLAN.md top to bottom (unchanged from prior runs, still pending your action):
- **Google Search Console verification** — verify site ownership and submit `sitemap.xml` once it exists (Phase 1). Needs your Google login.
- **Clutch / GoodFirms / G2 company profiles** — create and verify company profiles (Phase 5). Needs your account/company verification.
- **Client review solicitation** — ask existing case-study clients (MPL, Angara, QualityLabs, Creatiosoft) for reviews on the above platforms once they exist (Phase 5).

## Next up

The next unchecked task in PLAN.md is: `[AUTO]` Create `robots.txt` and `sitemap.xml`, verify all 5 URLs resolve with no broken internal links or 404s sitewide — a good candidate for tomorrow's run.
