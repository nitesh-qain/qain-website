# Daily SEO Report — 2026-09-20

## Shipped today

**Task:** `[AUTO]` Add JSON-LD structured data: `Organization` sitewide, `FAQPage` on the homepage FAQ section, `Article` on each case study. (Phase 1)

- Added an `Organization` JSON-LD block (name, url, logo, email, phone, LinkedIn `sameAs`) to the `<head>` of all 5 pages: `index.html`, `case-studies/index.html`, `case-studies/real-money-gaming.html`, `case-studies/contract-to-hire.html`, `case-studies/automation.html`.
- Added a `FAQPage` JSON-LD block to `index.html` with all 5 Q&A pairs from the existing `#faq` section — question/answer text copied verbatim from the visible content (HTML entities converted to plain text, no rewording), per Google's structured-data matching guidance.
- Added an `Article` JSON-LD block to each of the 3 case study pages (`real-money-gaming.html`, `contract-to-hire.html`, `automation.html`) with headline/description sourced from each page's existing `<title>`/meta description, `datePublished` taken from the publish dates already shown in the homepage blog list (2025-11-24 for the two 2025 posts, 2024-05-08 for the Angara automation case study), and `author`/`publisher` set to the QAInfinity organization — no individual author name exists anywhere on these pages, so nothing was invented.
- Did **not** add `Article` to `case-studies/index.html`, since it's a listing/hub page, not an article itself (still got the `Organization` block).

**Commit:** [`44e2471`](https://github.com/nitesh-qain/qain-website/commit/44e2471) — pushed directly to `main`.

**Verification:** Ran a real headless-Chromium pass (Playwright, via the pre-installed sandbox browser) against all 5 pages served from a local static server:
- All 5 pages returned HTTP 200 with no new console errors introduced by this change.
- Zero broken internal links found on any page.
- All 8 JSON-LD `<script>` blocks across the 5 pages parsed as valid JSON with the expected `@type` (`Organization` ×5, `FAQPage` ×1, `Article` ×3).
- Diffed the change to confirm it's purely additive (202 insertions, 0 deletions) — nothing else on any page was touched.
- Two pre-existing items, unrelated to this change, were double-checked and are **not** regressions: (a) console errors from Google Tag Manager / Google Fonts are caused by the sandbox's network egress policy blocking those third-party domains, present before this change too; (b) a mobile-viewport overflow flag on `index.html` and `automation.html` was reproduced identically against the pre-edit versions of those files (via `git stash`), confirming it already existed and wasn't introduced today — worth a look during the upcoming Core Web Vitals task (Phase 1), not something this run should silently "fix" outside its scope.

## Opened for review

None today — no `[PR]` blog task was reached (the next unchecked task in PLAN.md was the `[AUTO]` structured-data item above).

## Blocked / not shipped

None — the task completed and verified successfully on the first attempt.

## Research findings

Not a Phase 3 research day.

## LinkedIn draft

Not applicable — no blog post or notable milestone shipped today (this was a technical SEO / structured-data task, not customer-facing content).

## Reminders — `[NEEDS-YOU]` items skipped

Passed over while scanning PLAN.md top to bottom (unchanged from prior runs, still pending your action):
- **Google Search Console verification** — verify site ownership and submit `sitemap.xml` once it exists (Phase 1). Needs your Google login.
- **Clutch / GoodFirms / G2 company profiles** — create and verify company profiles (Phase 5). Needs your account/company verification.
- **Client review solicitation** — ask existing case-study clients (MPL, Angara, QualityLabs, Creatiosoft) for reviews on the above platforms once they exist (Phase 5).

## Next up

The next unchecked task in PLAN.md is: `[AUTO]` Add JSON-LD `Review`/testimonial markup for the homepage testimonials section — a good candidate to pair with tomorrow's run, or the `robots.txt` + `sitemap.xml` task right after it.
